надо очень внимательно следить за тем чтобы у колонок был задан тип (при использовании таблицы значений полученную при использовании запроса все типы колонок проставляются автоматически, это важно при ручном создании и заполнении таблицы значений)

Чтобы поместить таблицу значений в запрос, придется загрузить ее сначала во временную таблицу, а уже потом соединять с нужными данными.

[[ПРИМЕР 2]]
